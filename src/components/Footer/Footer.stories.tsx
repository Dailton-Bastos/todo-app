import { Footer } from '.'

const config = {
	title: 'Footer',
	component: Footer,
	tags: ['autodocs'],
	parameters: {
		layout: 'fullscreen',
	},
}

const Default = {
	args: {},
}

export { Default }

export default config
